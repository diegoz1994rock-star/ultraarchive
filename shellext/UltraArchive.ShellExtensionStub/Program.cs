// El esquema del AppxManifest exige un <Application> con un ejecutable de punto de entrada.
// Este stub nunca se lanza: la DLL COM (UltraArchive.ShellExtension.dll) corre en un proceso
// surrogate del sistema. Solo está aquí para que el paquete MSIX tenga identidad de aplicación.
return 0;
